# ATM4171 2026 term project

Suggested directory structure:

```
- notebooks/
    - data/
        - test.csv
        - train.csv
    - outputs/
        - (any plots and other outputs that need to be reported)
    - temp/
        - (Temporary outputs can be saved here to keep /notebook and /outputs dirs from getting cluttered)
- src/
    - (Any code that we may want to re-use in the notebooks)
```

Shared notebooks:
- report_draft.ipynb: For a draft version of the report.
- report_final.ipynb: Final version of the report (let's keep this clean).
